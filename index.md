<head>
    <link rel="stylesheet" type="text/css" href="index.css">
</head>
<body>
<p style='font-size:120%; font-weight:bold'>توانایی دسترسی آفلاین به وب‌سایت‌ها و وب‌اپ‌ها</p>
<p>امکان دسترسی آفلاین به سایت‌ها و اپ‌ها، یک خصوصیت است؛ برای مثال این خصوصیت در دستگاه‌های موبایل FormAgent در حالت آفلاین اجازه‌ی استفاده را به ما می‌دهد و هنگام دسترسی به اینترنت داده‌ها را همگام می‌کند. عموما در اکثر اپ‌ها و سایت‌ها، فعالیت عمده بر توسط سرور صورت می‌گیرد؛ پایگاه داده‌ در سرور قرار دارد و تغییر و نمایش داده‌ها نیز توسط سرور صورت می‌گیرد. برای افزایش توانایی دسترسی به آن‌ها به صورت آفلاین، بایستی فعالیت سرور را تا حد امکان به کلاینت منتقل کرد.</p>

<p>روش‌های مختلفی برای طراحی پروژه با امکان دسترسی آفلاین وجود دارد.
 ۱- جدا کردن اپ از سرور: اضافه کردن ساختار MVC به کلاینت به جای سرور و صرفا قرار دادن داده‌ها در سرور
۲- ساختن یک API object پوششی، به طوری که عملکرد API اصلی را شبیه‌سازی کند. این کار از درخواست‌های AJAX مستقیم جلوگیری می‌کند و امکان درخواست داده را در سایر بخش‌های اپ بدون دانستن جزئیات پیاده‌سازی، فراهم می‌کند.
۳- جداسازی به‌روزرسانی داده و ذخیره‌سازی‌ آن:استفاده از یک object کنترل‌کننده برای ذخیره‌سازی داده و برنامه‌ریزی برای به‌روزرسانی‌ داده ها که به عنوان یک proxy عمل می‌کند و تمامی درخواست‌های داده از طریق آن صورت می‌گیرد.</p>

<p>در حالت معمول، می‌توان برای دسترسی به داده‌ها مستقیما به سرور درخواست داد. ولی برای این که به داده‌ها دسترسی بیش‌تری در حالت آفلاین داشته باشیم، بایستی همان‌طور که در بالا به صورت توضیح داده شد،  ذخیره‌سازی و به‌روزرسانی داده‌ها را از هم جدا کنیم. این کار با قرار دادن یک proxy بین API object و سایر قسمت‌های اپ امکان‌پذیر است؛ که وظیفه‌ی آن به‌روزرسانی داده‌ها در API و پردازش پاسخ‌هاست.</p>

<p>یک سایت برای این که بتواند دسترسی آفلاین را پشتیبانی کند، بایستی به دو مؤلفه توجه کند.
۱- دارایی‌های اپ: cache کردن CSS، HTML، JS و تصاویر.
۲- داده‌های اپ: ذخیره‌سازی داده‌ها در کلاینت.(روش‌هایی که در بالا توضیح داده شد، مربوط به این مؤلفه است.)
داده‌های اپ می‌توانند در حافظه‌ی آفلاین مرورگر ذخیره شوند. HTML5 روش‌های مختلفی را برای این کار دارد. علاوه بر این کتاب‌خانه‌های مختلفی برای ذخیره‌سازی آفلاین داده‌ها فراهم شده است.</p>

</body>
